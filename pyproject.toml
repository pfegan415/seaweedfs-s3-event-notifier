[project]
name = "seaweedfs-s3-event-notifier"
version = "0.1.0"
description = "An app which receives event notifications from SeaweedFS and formats each message to the schema of an AWS S3 event before sending to an AWS SQS compatible message queue"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "blacksheep>=2.6.2",
    "granian>=2.7.4",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
    "pytest-asyncio>=1.3.0",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
